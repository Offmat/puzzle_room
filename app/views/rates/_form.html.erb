<%= form_for @rate, html: {class: "inline"} do |f| %>
  <%= f.hidden_field :puzzle_id, value: @puzzle.id %>

    <%= f.number_field :rating, in: 0..10, class: "number-input" %>

  <%= f.submit "Rate", class: "btn btn-primary btn-xs" %>
<% end %>
